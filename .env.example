# Backend deploy / local development

# Desktop client repo (DMG/MSI uploads during deploy_prod.sh)
DESKTOP_REPO=../HRCoach
# Override artifact search path if installers are elsewhere
# ARTIFACTS_DIR=/path/to/installers

# Landing build (see landing/.env.local.example)
# NEXT_PUBLIC_APP_URL=
# NEXT_PUBLIC_LICENSE_API_URL=
