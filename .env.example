# Backend deploy / local development (never commit real secrets)

# Used by deploy_prod.sh when uploading desktop installers (optional)
ARTIFACTS_DIR=../artifacts

# Landing build (see landing/.env.local.example)
# NEXT_PUBLIC_APP_URL=
# NEXT_PUBLIC_LICENSE_API_URL=
